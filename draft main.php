
   
<body  ng-controller="TodoListCtrl">
	<!--
	<h2>Add new</h2>
	{{Form::open(['route' => 'tasks.store'])}}
		{{Form::label('task', 'Task')}}
		{{Form::text('content')}}
		{{Form::submit('Save!', array('class' => 'btn btn-default'))}}
	{{Form::close()}}
	-->


<!--



			<div class="col-md-1">{{Form::submit('T', array('class' => 'btn btn-info'))}}</div>

	<h2>Tomorrow</h2>
	@foreach($tomorrow as $t)
	<div>
		<div @if($t->urgent) class="urgent" @endif 
			@if($t->important) class="important" @endif >
			{{$t->content}}
		</div>
	</div>
	@endforeach

	<h2>Later</h2>
	@foreach($later as $t)
	<div>
		<div @if($t->urgent) class="urgent" @endif 
			@if($t->important) class="important" @endif >
			{{$t->content}}
		</div>
	</div>
	@endforeach

-->


